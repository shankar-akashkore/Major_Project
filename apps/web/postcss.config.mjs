/** Tailwind 4 is a PostCSS plugin and needs no config file of its own — the
 *  theme lives in `app/globals.css` next to the styles that use it. */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
